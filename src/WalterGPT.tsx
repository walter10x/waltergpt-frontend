 import { RouterProvider } from "react-router-dom"
import { Router } from "react-router-dom"
import { router } from "./presentation/routes/routes"


export const WalterGPT = () => {
  return (
   <RouterProvider router = {router} />
  )
}
